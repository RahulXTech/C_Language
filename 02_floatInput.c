#include<stdio.h>

int main(){

float a;
float b;
// if you give the input with space than it will auto detect second number.
scanf("%f%f", &a, &b);
printf("A value of a is: = %f, A value of  b is : = %f", a, b);

return 0;
}